[phases.setup]
nixPkgs = ["ffmpeg", "nodejs_22"]

[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build", "cp -r drizzle dist/drizzle"]

[start]
cmd = "pnpm run start"
